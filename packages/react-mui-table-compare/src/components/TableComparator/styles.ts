import { styled } from '@mui/material/styles';
import { Table, TableHead, TableFooter, Stack } from '@mui/material';

export const TableRoot = styled(Table)();

export const Head = styled(TableHead)();

export const Actions = styled(TableFooter)();

export const CompareViewRoot = styled(Stack)();
