import { reactRequiredContexts } from './react-required-contexts';

describe('reactRequiredContexts', () => {
  it('should work', () => {
    expect(reactRequiredContexts()).toEqual('react-required-contexts');
  });
});
